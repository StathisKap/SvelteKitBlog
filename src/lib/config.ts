import { dev } from '$app/environment';

export const title = 'SvelteBlog';
export const description = 'Sveltekit blog from tutorial by joy of code';
export const url = dev ? 'http://localhost:5173/' : '';
